# LiveMix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A lightweight live mic VST3 host for broadcast: ASIO in → per-channel VST3 chain → mic ON/OFF → master / direct outs, FX channels with pre/post sends, sessions, tray operation, WebDAV backup.

**Architecture:** A second app target in the Enqueue repo (`livemix/`), linking `GoCueCore` for VST3 hosting (`PluginHost`, `PluginChain`, `PluginWindows`), settings, updater and the verified file write. New code: `MixSession` (model + JSON), `MixEngine` (graph on one ASIO device, offline-renderable for tests), the card UI, tray, WebDAV backup.

**Tech Stack:** JUCE 8 (C++17, MSVC), Inno Setup, WinSparkle, headless Chrome mocks in `scratchpad/micrack`.

**Spec:** `docs/superpowers/specs/2026-09-04-livemix-design.md`

## Global Constraints

- ASIO only; the device list shows ASIO devices only; every input and output of the device is opened.
- No channel gain / fader: channels are unity. Mic ON/OFF is a 5 ms ramp and cuts the sends too.
- UI words: 마이크 채널 / 마이크 ON·OFF / VST3 체인 / 이펙터 / FX 채널 / 마스터 / 출력 / 세션 / 온라인 백업 (never 뮤트·게인·AUX·센드).
- Colours and fonts through one palette (`livemix/src/ui/LiveMixPalette.h`), never hard-coded in components.
- Every task ends with the full test suite green (`build/vs2022/tests/EnqueueTests_artefacts/Release/EnqueueTests.exe`) and a Codex read-only review before the release task.

---

### Task 1: Model, verified write, engine (offline)

**Files:**
- Create: `src/model/SafeFileWrite.h/.cpp` (the temp-verify-swap write extracted from `ProjectSerializer::save`)
- Modify: `src/model/ProjectSerializer.h/.cpp` (use it; expose `pluginSlotsToVar/FromVar`)
- Create: `livemix/src/MixModel.h/.cpp`, `livemix/src/MixEngine.h/.cpp`
- Modify: `CMakeLists.txt` (core source, `LiveMixCore` interface target), `tests/CMakeLists.txt`
- Test: `tests/MixSessionTests.cpp`, `tests/MixEngineTests.cpp`

**Interfaces:**
- `MixSession { name, device, channels[], fx[], master; sanitise(); toJson(); fromJson(); save(); load(); }` — ids are `juce::Uuid`, sends reference fx by id.
- `MixEngine { initialise(xml) (ASIO only), prepare(sr, bs), renderBlock(inputs, numIn, outputs, numOut, n), applySession(session, errors, forceRestore), captureLivePluginStates(session), setChannelOn/Input/Output, setSend, setFxReturn/Output, setMasterOutput, getChannelChain/getFxChain/getMasterChain, readChannelMeter/readFxMeter/readMasterMeter (max since last read), getLatencyMs, getDspLoad }`.

- [x] Write `SafeFileWrite::writeTextVerified (file, text, verifier)`; `ProjectSerializer::save` calls it (the same behaviour, the SafeSaveTests stay green).
- [x] Write `MixModel` with JSON round trip; tests: round trip of every field, sanitise (unknown fx send dropped, missing send added, clamps, duplicate ids), future version refused, foreign app refused, save/load atomic.
- [x] Write `MixEngine` with the graph in section 3 of the spec; tests with DC inputs: routing (master / direct / both), ON→OFF ramp reaches zero and cuts the sends, pre vs post send through a `TestGainPlugin`, FX return amount, master chain, stereo input pair, meters, applySession keeps existing chains (pointer identity) and drops removed channels.
- [x] Build, run the suite, commit.

### Task 2: App, window, cards, device

**Files:** `livemix/src/Main.cpp`, `livemix/src/ui/LiveMixPalette.h`, `LiveMixLookAndFeel.h`, `MainComponent.h/.cpp`, `TopBar.h/.cpp`, `ChannelCard.h/.cpp`, `MasterCard.h/.cpp`, `MeterBar.h/.cpp`, `SendRow.h/.cpp`; `CMakeLists.txt` (juce_add_gui_app LiveMix, icon `livemix/assets/icon_512.png`).

- [x] App shell: dark console palette from the mock, top bar (logo, session, ASIO device combo listing ASIO devices only, rate/buffer/latency, DSP, buttons), scrolling card list, docked master card, status bar.
- [x] ChannelCard: number + name (double-click edits, Enter/Esc), mic ON/OFF lamp button, input combo (mono n / stereo n-n+1 from the device's input names), chain summary chips ①→②→③ + "체인 열기" + "+ 추가", send rows per FX (big slider + % + 프리/포스트), output chips (마스터 toggle, 직접 출력 pair combo), meter. Card dims when OFF.
- [x] Responsive: ≥1180 four columns, <1180 two rows, <800 one column; the list scrolls; the master stays docked.
- [x] Engine wiring: the document (`MixSession` + dirty flag + undo-less edits) drives the engine through the live setters; a 30 Hz timer pulls meters / DSP / latency.
- [x] Build, run, commit.

### Task 3: Panels and plugins

- [x] Chain drawer (right side): vertical rows with drag handle (reorder = `PluginChain::movePlugin`), on/bypass switch, 열기 (PluginWindowManager), delete, "+ 플러그인 추가" menu grouped by manufacturer (reuse the Enqueue menu builder), a scan entry.
- [x] FX drawer: tabs per FX (add up to 4, double-click rename, delete), chain summary + open, return amount slider, outputs, meter, "이 FX로 보내는 마이크" list.
- [x] Build, run, commit.

### Task 4: Sessions, tray, settings

- [x] Session menu: new / open / save / save as / recent; autosave 10 s after a change and on quit; `.livemix` association; reopen the last session and start audio on launch; plugin states captured on save.
- [x] Tray: minimise (and optionally close) to tray, double-click restores, menu (전체 마이크 ON/OFF, 열기, 종료). Start with Windows (Run key) setting.
- [x] Settings dialog: ASIO device, buffer (ASIO control panel), tray, autosave, backup (WebDAV URL, folder, user, remember password).
- [x] Build, run, commit.

### Task 5: Online backup

- [x] Dialog: 크리에이터 이름 (remembered) + password when not stored; save session first; upload `/LiveMix 백업/<PC>/<creator>_<yyyy-MM-dd_HHmmss>.livemix` with `juce::URL` PUT + Basic auth on a background thread (MKCOL for the folders); status line with the result. Tests for the path builder / name sanitising.
- [x] Build, run, commit.

### Task 6: Release

- [x] Installer `installer/LiveMix.iss` (own AppId, folder, association), `tools/release.py --app livemix` (product name, exe, installer name, appcast, repo `dnakrhs2-crypto/livemix`, site page `site/livemix/`), WinSparkle initialised with its own appcast, About with the VST trademark line.
- [x] Codex review round 5 (14 LiveMix findings, all addressed): MainComponent detaches the chain listeners / closes dialogs before the window manager dies; ChainDrawer reorder and bypass deferred (callAsync + SafePointer); `withSessionSecured()` guards new / open / quit (save, or ask 저장 / 저장 안 함 / 취소), `autosaveNow()` reports failure and the backup refuses on it, the updater's canShutdown waits for a clean document and no upload; MixDocument clean after new / load / save, `markDirty(false)` for plugin-editor tweaks polled at 30 Hz; `applySession` builds every node and the master chain off the graph on a load and swaps once (no allocation under the lock), master restored only on load; `openSessionDevice()` opens the session's device on load with a fallback warning and the session keeps its device name; one send snapshot per block; safe mode (Shift / --safe-mode); WebDavBackup is an owned cancellable thread, PUT to .part + MOVE, https only, credentials keyed by server + user; trailing JSON data refused; release.py repo / remote / tag from the app table, a gh failure never wipes the other app's site links. Tests: MixDocumentTests, MixEngineTests (stale master, off-graph rebuild), MixSessionTests, WebDavBackupTests.
- [x] Codex rounds 6-8 (the fixes), 0.1.0 released 2026-09-04 (livemix-v0.1.0), installed here, Desktop copy, memory updated, reported.
- [x] 0.1.1: FX 채널 모노 스위치 (gom 요청 2026-09-04) — `MixFx::mono`(JSON "mono"), `MixEngine::setFxMono` + 렌더에서 리턴 뒤 (L+R)/2를 양쪽에, `MixDocument::setFxMono`, FxDrawer 출력 칸 '모노' 칩. 테스트 MixEngineTests/MixSessionTests. 코덱스 리뷰 후 배포.
- [x] 0.1.2: 창 크기 조절이 안 되던 원인 = 모달 AlertWindow(ASIO 실패·세션 경고·안전 모드) 동안 JUCE가 본 창의 비클라이언트 클릭까지 삼킴(`sendInputAttemptWhenModalMessage`) → 창 안 알림 띠(`MainComponent::showNotice/hideNotice`)로 교체, 실제 마우스 드래그로 검증(1456→1656, 모서리 1206×699). 교훈: 라이브 도구에서 시작 시 모달 금지.
- [x] 0.1.2 두 번째 패스 (코덱스 알림 띠 리뷰 5건 + gom "창 줄이면 멈춤" 추적): ①★두 번째 실행 크래시 — JUCE는 `initialiseApp()`이 false(다른 인스턴스 있음)여도 `shutdown()`을 부르는데 `settings` 널 역참조(오늘 이벤트 로그 0xc0000005 3회) → `settings == nullptr` 가드 ②명령줄 세션 실패 시 마지막 세션 폴백 금지(`openFromCommandLine` bool) ③`addNotice`로 시작 안내를 세션 경고에 이어붙임 ④저장 실패 = 띠 ⑤띠 본문 = 읽기 전용 TextEditor(줄바꿈·긴 토큰 분할·5줄 넘으면 스크롤, 높이는 에디터 실측) ⑥오른쪽 아래 `ResizableCornerComponent` 손잡이(MainWindow가 제한을 먼저 설정) ⑦두 번째 실행 시 모달 질문을 앞으로. 마우스 드래그 재현: 설치본 0.1.1·0.1.2 빌드 모두 최소(720×560)까지 줄였다 다시 늘리기 정상 — gom은 RustDesk로 조작(cm 로그 13:25·14:45).
- [x] 0.1.2 코덱스 2차 리뷰(High 0, Medium 2, Low 2) 반영: 모달(이름 바꾸기 등) 열린 채 세션 파일 더블클릭 → `pendingCommandLineFile`로 대기 후 타이머에서 열기 / 저장 실패는 `saveErrorNote` 별도 줄(세션·장치 경고 안 덮음, 수동·다른 이름·종료 전·자동 저장 성공 시 그 줄만 제거) / 없는 .livemix 인자도 "지정된 세션"으로 취급(파일 없음 안내, 폴백 없음).
- [x] 0.1.3 온라인 백업 창 (gom 9/4 지시: 목록·올리기·내려받기 전부 아이디/비번, 관리자는 전부 — 그런 문구는 UI에 넣지 않음): `WebDavBackup` 3작업(upload/list/download, PROPFIND 파서 `parseMultistatus`·`parseHttpDate`, 홈 폴더 `/home/<폴더>`·관리자 `/homes/<계정>/<폴더>`), `BackupDialog` = 비모달 창(아이디·비밀번호·이 PC에 기억·목록 표·크리에이터 이름·지금 세션 백업·선택한 백업 불러오기), 설정에서 사용자·비밀번호 기억 제거. 서버 권한 구조 확인 결과 0.1.4에서 앱 계정 방식으로 바뀜(운영 메모는 비공개 메모리에).
- [x] 0.1.3 코덱스 리뷰(High 0, Medium 4, Low 4) 반영: 목록에서 403/404/405만 "없음", 그 외 상태·읽을 수 없는 XML은 오류 / 끊긴 응답(getTotalLength 대조) 오류 / 내려받은 파일은 `MixSession::load`로 검증 후 열기 / 창이 닫혀도 성공한 작업은 계정 저장·삭제 반영(`persistAccount`) / 업로드 뒤 목록 갱신은 80 ms 뒤(워커 스레드 종료 대기) / 다운로드 완료는 자기 창만 닫음 / '+' 보존 디코딩 / 빈 폴더 설정=`/home` / GET 403은 권한 문구. 창 제목이 세션 열기·새 세션에서 안 바뀌던 것도 수정(`notifyValue`).
- [x] 0.1.4 앱 계정 (gom 9/4 "시놀로지는 하나의 권한으로 하고 프로그램에서 계정을 만들라는 이야기"): 서버 쪽은 전용 계정 하나 + 전용 공유 폴더 하나(그 폴더만 접근)를 CMake `LIVEMIX_BACKUP_ACCOUNT_FILE`(저장소 밖 JSON, 비밀번호는 hex)로 앱에 내장(`BackupServer.h`). 계정 = `<share>/accounts/<id>.json`(salt + SHA-256 20000회, `If-None-Match: *`로 생성 전용), 관리자 = `accounts/admins.txt`, 백업 = `<share>/<id>/<PC>_<날짜시간>.livemix`. 작업 4개(createAccount/signIn/upload/download) 모두 계정 비밀번호 검증 후 진행, 다른 계정 백업 내려받기는 관리자만. 창: 아이디·비밀번호·이 PC에 기억·로그인·계정 만들기·목록·지금 세션 백업·불러오기. 설정의 주소·폴더 칸 제거. ★저장소가 공개라 서버 계정·폴더·관리자 이름은 소스와 이 문서에 넣지 않음(운영 메모는 비공개 메모리에).
- [x] 0.1.4 코덱스 리뷰(High 1: 공개 트리에 서버 이름 노출 → 테스트 픽스처·문서를 가상 이름으로, Medium 3, Low 2) 반영: 내려받기 경로는 `parseBackupPath`로 `<share>/<owner>/<file>.livemix` 정확 일치(.. 등 거부, 목록도 동일 검사) / 생성 전용 PUT 성공 = 계정 생성(폴더는 best effort) / 창이 닫혀도 계정 생성 결과 저장·보고 / 404만 "없음", 401·403·기타는 오류(관리자 목록 포함) / 다음 작업은 `whenIdle` 폴링으로 워커 종료 후 연결.
- [x] 0.1.5 (gom 9/4 "채널 OFF하면 플러그인도 점유율 없어지게, 설정에서 온오프"): `MixEngine::setSkipChainWhenOff`(기본 켬) — 램프를 먼저 계산해 완전히 OFF인 채널은 체인 처리를 건너뛰고 미터엔 원음 레벨, 설정 토글 `skipPluginsWhenOff`(SettingsDialog, Main.cpp에서 시작 시 적용). 테스트: OFF 동안 processCount 정지·끄면 계속·ON 복귀. 케이크워크 아카이브(A)와 같은 효과; 단점(켤 때 지연 플러그인 잔상)은 설정 문구에 명시.
- [x] 0.1.6: 설정 토글 문구 gom 지정 "OFF시 플러그인 OFF"(설명 문구 없이).
- [x] 0.1.7 (gom 9/4 "자동저장 10초는 너무 과함, 메뉴 없애"): 주기 자동 저장·설정 칸·`autosaveSeconds` 제거. `autosaveNow`→`saveIfDirty`(종료 시·백업 직전에만). 종료 시 조용히 저장하는 동작은 유지(gom에게 보고).
- [x] 0.1.8 (gom 9/4 저녁 요청 묶음): '+ 추가' 메뉴를 누른 버튼 옆에(anchor = 버튼; 채널 카드는 nullptr→서랍이었음), 전체 글씨 x1.2(`pt()`·`textScale`, LiveMixPalette.h), FX 기본 이름 중복 표기 제거, Ctrl+S/Ctrl+Shift+S/Ctrl+N(MainComponent가 KeyListener, MainWindow에 등록), '?'→'정보', DSP→CPU, ★트레이 검은 아이콘: 원인은 크기가 아니라 Windows 11 알림 영역이 메모리 비트맵으로 만든 HICON(JUCE `createHICONFromImage`의 BI_BITFIELDS DIB든 순수 GDI BI_RGB DIB든, 알파 유무 무관)을 검은 사각형으로 그리는 것(이 PC 실측: JUCE 아이콘·GDI 아이콘 모두 검정, PNG `CreateIconFromResourceEx`는 안 보임). 해법 = exe의 아이콘 리소스를 `ExtractIconEx`로 꺼내 JUCE 트레이 항목(hWnd+uID 동일)에 `Shell_NotifyIcon(NIM_MODIFY)`로 교체 → 빨간 ON 정상. 네이티브 제목표시줄의 최소화 버튼은 `minimiseButtonPressed`에 안 오므로 `minimisationStateChanged`에서 트레이로.
- [x] 0.1.9 안정성 묶음(코덱스 전체 리뷰 H4·M7·L2 전부 반영): 엔진 그래프는 세션 적용 전까지 비움(`MixDocument` 생성자 미적용, `applyToEngine`), `PluginChain::process`는 플러그인 콜백 락 try-lock(바쁘면 드라이), `audioDeviceAboutToStart`는 `isOpen()`일 때만 running·`audioDeviceError`는 running 해제·`openSessionDevice`는 열려 있지 않으면 재오픈, NaN/Inf → 드라이+faulted+`takeNewFaults` 알림 줄, prepare/release/get·setState/createEditor/createInstance try/catch, 컨트롤 패널 restart → `restartDevice`, 저장 재시도(2회), 센드/리턴 램프(`addFromWithRamp`/`applyGainRamp`, 새 노드는 세션 값에서 시작), 지연 플러그인 경고 줄(1초마다), 세션 32 MB·체인 16개·WebDAV 응답 64 MB 상한, stopThread 8 s, MIDI 버퍼 예약, 장치 채널 이름 64개 상한. 테스트: NaN 격리·락 바쁨 드라이·램프·빈 그래프·크기 상한·prepare 예외.
- [x] 0.1.8 코덱스 리뷰 5건 → 0.1.9에 합침(0.1.8은 미배포): Ctrl+S/N 전에 편집 중인 이름 커밋(`hideEditor(false)`), '+ 추가' 앵커를 서랍 열기 전 화면 좌표로 캡처(`showAddMenu(Rectangle)`), 칩 줄 수를 실제 흐름으로 계산(`ChipFlow`, 카드·마스터·FX 서랍, 마스터 높이 `getPreferredHeight(width)`), FX 탭 기본 이름=배지, `drawToggleButton` 글씨 배율.
- [x] 0.2.0 뮤트그룹(gom 9/5): 세션 `muteGroup` 마크(채널·FX), 엔진 `muted` 원자값(채널=OFF 램프, FX=리턴 0), `MuteGroups` 컨트롤러(토글·apply·reset, 세션 열기/새 세션에 reset), `GlobalHotkeys`(메시지 전용 창 + RegisterHotKey, F키/숫자패드/조합키만), 설정 대화상자 핫키 캡처 2줄, 카드·FX서랍 '뮤트그룹' 칩, 탑바 빨간 배지, 트레이 메뉴 토글.
- [x] 0.2.0 세로 모드(gom 9/5): TopBar `modeFor/preferredHeight`(wide/compact<1000/narrow<700: 1·2·3줄), MasterCard `narrowBelow=760` 스택 레이아웃+높이, 서랍은 700 미만에서 전체 폭, 창 최소 폭 720→420.
\n- [x] 0.3.0(gom 9/5 00:50): VST2 호스팅(SDK 저장소 밖 `GOCUE_VST2_SDK_DIR`, `JUCE_PLUGINHOST_VST`, `PluginHost::setVst2Enabled` 기본 끔 — 메뉴·생성 모두 차단), 메뉴바(`MenuBarModel`: 세션/온라인 백업/설정/도움말), 탑바 오른쪽 FX 채널+플러그인 관리, `PluginManagerWindow`(사용 스위치=`disabledPlugins` 키, 스캔은 숨긴 `PluginListComponent::scanFor`, 프리셋 표+빌더), `PluginPreset`(JSON, `Documents/LiveMix/플러그인 프리셋`), ChainDrawer '+ 추가'에 프리셋 불러오기(바꾸기/뒤에 추가)·체인을 프리셋으로, 백업 `startUploads`/`presetPathFor`/`Entry::isPreset`, 계정 등록 창(등록완료!→확인).\n